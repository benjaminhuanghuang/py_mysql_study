* Install Docker
 https://www.docker.com/products/docker
 
 
  
* Build new env:
 
    $ docker-compose up
 
* Remove or clean ENV:
    $ docker-compose down


* Start and stop service

    $ docker-compose start
    $ docker-compose stop
 
  
* List Docker services  
    $ docker ps
 